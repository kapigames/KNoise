{
    "tasks": [
        {
            "type": "shell",
            "label": "Build",
            "command": "mkdir build;cd build;cmake ../;cmake --build .",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}/"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Configure and build."
        }
    ],
    "version": "2.0.0"
}